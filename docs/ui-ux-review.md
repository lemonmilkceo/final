# 🎨 UI/UX 점검 및 개선안

## 싸인해주세요 (SignPlease)

> **분석 도구**: UI/UX Pro Max Skills  
> **분석일**: 2026년 2월 2일  
> **현재 버전**: UI_Spec v1.18

---

## 1. 디자인 시스템 분석 결과

### 1.1 UI/UX Pro Max 권장 디자인 시스템

SignPlease의 특성(B2B SaaS, 법률 계약, 한국 시장)을 분석한 결과:

| 항목              | 권장값                      | 현재 상태           | 평가                  |
| ----------------- | --------------------------- | ------------------- | --------------------- |
| **스타일**        | Trust & Authority + Minimal | Toss-style Minimal  | ✅ 적합               |
| **Primary Color** | #0369A1 (진한 블루)         | #3182F6 (토스 블루) | ✅ 유사               |
| **Background**    | #F8FAFC                     | #FFFFFF             | ⚠️ 약간 차이          |
| **Typography**    | Plus Jakarta Sans           | Pretendard          | ✅ 적합 (한국어 최적) |
| **접근성**        | WCAG AAA                    | 부분 적용           | ⚠️ 개선 필요          |

### 1.2 현재 잘 되어있는 점 ✅

| 항목              | 상태    | 설명                                                   |
| ----------------- | ------- | ------------------------------------------------------ |
| **색상 팔레트**   | ✅ 우수 | 토스 스타일 블루 기반, 시맨틱 색상 체계                |
| **타이포그래피**  | ✅ 우수 | 명확한 계층 (hero → title → subtitle → body → caption) |
| **버튼 컴포넌트** | ✅ 우수 | loading 상태, disabled 처리 완비                       |
| **애니메이션**    | ✅ 우수 | 부드러운 전환 (0.2-0.3s), 키프레임 정의됨              |
| **Safe Area**     | ✅ 우수 | iOS 노치/인디케이터 대응 완료                          |
| **카드 컴포넌트** | ✅ 우수 | interactive 상태에서 cursor-pointer 적용               |

---

## 2. 개선이 필요한 항목

### 2.1 🔴 CRITICAL - 접근성 (Accessibility)

#### 이슈 1: `prefers-reduced-motion` 미지원

**현재 상태**: 코드베이스에서 `prefers-reduced-motion` 사용 0건

**문제점**:

- 전정기관 장애가 있는 사용자에게 애니메이션이 어지러움 유발
- WCAG 2.1 Level AAA 미준수

**개선안**:

```css
/* globals.css에 추가 */
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

---

#### 이슈 2: `cursor-pointer` 누락

**현재 상태**: 컴포넌트에서 `cursor-pointer` 사용 1건만 (Card.tsx)

**문제점**:

- 클릭 가능한 요소임을 시각적으로 알 수 없음
- UX 신뢰도 저하

**영향받는 컴포넌트**:

| 컴포넌트      | 파일                                   | 상태    |
| ------------- | -------------------------------------- | ------- |
| ContractCard  | `components/contract/ContractCard.tsx` | ❌ 누락 |
| Header 버튼들 | `components/layout/Header.tsx`         | ❌ 누락 |
| BottomNav     | `components/layout/BottomNav.tsx`      | ❌ 누락 |
| FAB           | `components/layout/FAB.tsx`            | ❌ 누락 |
| FolderTabs    | `components/folder/FolderTabs.tsx`     | ❌ 누락 |

**개선안**:

```tsx
// ContractCard.tsx - 186번 라인
<button
  onClick={handleClick}
  className={clsx(
    'w-full bg-white rounded-2xl p-4 text-left transition-all cursor-pointer',
    // ... 나머지
  )}
>
```

---

#### 이슈 3: `aria-label` 부족

**현재 상태**: 7개 aria-label만 사용 (Header, FAB 등 일부)

**누락된 곳**:

| 요소        | 위치                    | 필요한 aria-label |
| ----------- | ----------------------- | ----------------- |
| 수정 버튼   | ContractCard            | "계약서 수정"     |
| 체크박스    | ContractCard (편집모드) | "선택"            |
| 닫기 버튼   | BottomSheet             | "닫기"            |
| 서명 캔버스 | SignatureCanvas         | "서명 입력 영역"  |

---

#### 이슈 4: Focus 상태 가시성

**현재 상태**: Input 컴포넌트에만 focus 스타일 적용 (24건)

**문제점**:

- 키보드 내비게이션 시 현재 위치 파악 어려움
- 버튼, 카드 등에 focus ring 없음

**개선안**:

```css
/* globals.css에 추가 */
button:focus-visible,
a:focus-visible,
[role='button']:focus-visible {
  outline: 2px solid #3182f6;
  outline-offset: 2px;
}
```

---

### 2.2 🟡 HIGH - 인터랙션 개선

#### 이슈 5: 호버 상태 일관성

**현재 상태**:

- 버튼: `active:` 상태만 있음 (모바일용)
- 데스크톱에서 호버 피드백 없음

**개선안** (데스크톱 호버 추가):

```css
/* globals.css 버튼 섹션에 추가 */
@media (hover: hover) {
  .btn-primary:hover:not(:disabled) {
    background-color: #2563eb;
  }

  .btn-secondary:hover:not(:disabled) {
    background-color: #e5e7eb;
  }

  .card:hover {
    background-color: #f9fafb;
  }
}
```

---

#### 이슈 6: 이모지 아이콘 사용

**현재 상태**: 일부 이모지가 UI 아이콘으로 사용됨

| 위치          | 이모지 | 권장 변경                     |
| ------------- | ------ | ----------------------------- |
| Header 크레딧 | 💎     | Lucide `gem` 또는 `coins` SVG |
| Input 에러    | ⚠️     | Lucide `alert-circle` SVG     |
| Header 인사말 | 👋     | 제거 또는 SVG 손 아이콘       |

**이유**:

- 이모지는 OS/브라우저별 렌더링 차이
- 전문적인 느낌 저하
- 접근성 도구에서 일관성 없는 읽기

---

### 2.3 🟢 MEDIUM - 디자인 일관성

#### 이슈 7: 배경색 불일치

**현재 상태**:

- 대부분 페이지: `bg-white`
- 권장: `bg-gray-50` (#F9FAFB) 배경에 `bg-white` 카드

**개선안**:

- 메인 컨테이너 배경을 `bg-gray-50`으로 변경
- 카드/섹션은 `bg-white`로 구분

```tsx
// 페이지 레이아웃
<div className="min-h-screen bg-gray-50">
  <Header />
  <main className="px-5 py-6">
    <div className="bg-white rounded-2xl p-5">{/* 콘텐츠 */}</div>
  </main>
</div>
```

---

#### 이슈 8: 트랜지션 속도 불일치

**현재 상태**:

- `transition-colors`: 기본 150ms
- `transition-all duration-300`: 300ms
- 혼재되어 사용

**개선안** - 일관된 속도 체계:

| 용도                              | 속도  | 클래스                           |
| --------------------------------- | ----- | -------------------------------- |
| 마이크로 인터랙션 (색상, opacity) | 150ms | `transition-colors duration-150` |
| 레이아웃 변화 (크기, 위치)        | 200ms | `transition-all duration-200`    |
| 모달/시트 애니메이션              | 300ms | `duration-300`                   |

---

### 2.4 🟢 LOW - 추가 개선사항

#### 이슈 9: 스켈레톤 로딩 미사용

**현재 상태**: 로딩 시 빈 화면 또는 스피너만 표시

**개선안**: 스켈레톤 컴포넌트 추가

```tsx
// components/ui/Skeleton.tsx
export function Skeleton({ className }: { className?: string }) {
  return (
    <div className={clsx('animate-pulse bg-gray-200 rounded', className)} />
  );
}

// 사용 예시
<Skeleton className="h-20 w-full rounded-2xl" />;
```

---

#### 이슈 10: 이미지 최적화

**현재 상태**: `loading="lazy"` 미사용

**개선안**:

- 스크롤 아래 이미지에 lazy loading 적용
- Next.js Image 컴포넌트의 `priority` prop 활용

---

## 3. 개선 우선순위

### Phase 1: Critical (즉시 적용)

| 항목                   | 작업                           | 예상 시간 |
| ---------------------- | ------------------------------ | --------- |
| prefers-reduced-motion | globals.css에 미디어쿼리 추가  | 5분       |
| cursor-pointer         | 모든 클릭 가능 요소에 추가     | 30분      |
| focus-visible          | globals.css에 전역 스타일 추가 | 10분      |

### Phase 2: High (다음 스프린트)

| 항목               | 작업                           | 예상 시간 |
| ------------------ | ------------------------------ | --------- |
| 데스크톱 호버 상태 | 미디어쿼리로 hover 스타일 추가 | 30분      |
| aria-label 보완    | 누락된 컴포넌트에 추가         | 1시간     |
| 이모지 → SVG       | Lucide 아이콘으로 교체         | 1시간     |

### Phase 3: Medium (선택적)

| 항목          | 작업                   | 예상 시간 |
| ------------- | ---------------------- | --------- |
| 배경색 체계   | 레이아웃 배경 변경     | 2시간     |
| 트랜지션 통일 | duration 클래스 정리   | 1시간     |
| 스켈레톤 로딩 | Skeleton 컴포넌트 제작 | 1시간     |

---

## 4. 체크리스트 (Pre-Delivery)

UI/UX Pro Max 권장 체크리스트:

### 비주얼 품질

- [ ] 이모지를 UI 아이콘으로 사용하지 않음 (SVG 사용)
- [ ] 모든 아이콘이 일관된 크기 (w-6 h-6 등)
- [ ] 호버 상태가 레이아웃 시프트를 일으키지 않음
- [ ] 브랜드 로고가 올바른 SVG 사용

### 인터랙션

- [x] 모든 버튼에 loading 상태 지원
- [ ] 모든 클릭 가능 요소에 `cursor-pointer`
- [ ] 호버 상태에 시각적 피드백 (색상, 그림자, 테두리)
- [ ] 부드러운 트랜지션 (150-300ms)
- [ ] 키보드 내비게이션 시 focus 상태 보임

### 접근성

- [ ] 모든 이미지에 alt 텍스트
- [ ] 폼 입력에 label 연결
- [ ] 색상만으로 정보 구분하지 않음
- [ ] `prefers-reduced-motion` 지원
- [ ] 명암비 4.5:1 이상

### 레이아웃

- [x] safe-area 지원
- [ ] 반응형: 375px, 768px, 1024px, 1440px 테스트
- [x] 모바일에서 가로 스크롤 없음

---

## 5. 적용 예시 코드

### globals.css 추가 코드

```css
/* ================================================
   16. Accessibility Improvements
   ================================================ */

/* Reduced Motion */
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}

/* Focus Visible */
button:focus-visible,
a:focus-visible,
[role='button']:focus-visible,
input:focus-visible,
textarea:focus-visible,
select:focus-visible {
  outline: 2px solid #3182f6;
  outline-offset: 2px;
}

/* Desktop Hover States */
@media (hover: hover) {
  .btn-primary:hover:not(:disabled) {
    background-color: #2563eb;
  }

  .btn-secondary:hover:not(:disabled) {
    background-color: #e5e7eb;
  }

  .btn-kakao:hover {
    background-color: #e6cf00;
  }

  .card:hover {
    background-color: #f9fafb;
  }
}
```

---

## 6. 결론

### 현재 점수: **82/100** 🟢

| 카테고리            | 점수   | 비고      |
| ------------------- | ------ | --------- |
| 색상 & 타이포그래피 | 95/100 | 우수      |
| 레이아웃 & 반응형   | 90/100 | 우수      |
| 컴포넌트 품질       | 85/100 | 양호      |
| 애니메이션          | 80/100 | 양호      |
| 접근성              | 65/100 | 개선 필요 |
| 인터랙션 피드백     | 75/100 | 개선 필요 |

### 핵심 개선 3가지

1. **prefers-reduced-motion 지원** - WCAG 준수
2. **cursor-pointer 전면 적용** - 클릭 가능성 명확화
3. **이모지 → SVG 교체** - 전문성 향상

이 3가지만 적용해도 **90점 이상**으로 향상됩니다.

---

> **문서 끝**
