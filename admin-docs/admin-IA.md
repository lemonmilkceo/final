# DevSupport AI - IA (Information Architecture)

> **버전**: 1.0  
> **작성일**: 2026년 2월 6일  
> **기준 문서**: PRD.md

---

## 1. 전체 사이트맵

```
signplease.com
│
├── 🔓 공개 영역 (비로그인 접근 가능)
│   ├── /                         → 랜딩 페이지
│   ├── /login                    → 로그인 (카카오/애플)
│   ├── /signup                   → 회원가입
│   ├── /pricing                  → 가격 정책
│   ├── /guest                    → 게스트 모드 진입
│   └── /contract/sign/[token]    → 서명 페이지 (토큰 기반)
│
├── 🔒 보호 영역 (로그인 필수)
│   │
│   ├── /select-role              → 역할 선택 (사장님/직원)
│   │
│   ├── 📁 사장님 (Employer)
│   │   ├── /employer                      → 대시보드
│   │   ├── /employer/create               → 계약서 작성
│   │   ├── /employer/preview/[id]         → 계약서 미리보기
│   │   ├── /employer/contract/[id]        → 계약서 상세
│   │   ├── /employer/folders              → 폴더 관리
│   │   └── /employer/chat/[contractId]    → 채팅
│   │
│   ├── 📁 직원 (Worker)
│   │   ├── /worker                        → 대시보드
│   │   ├── /worker/contract/[id]          → 계약서 상세
│   │   ├── /worker/career                 → 경력 관리
│   │   ├── /worker/onboarding             → 온보딩
│   │   └── /worker/chat/[contractId]      → 채팅
│   │
│   ├── 📁 공통
│   │   ├── /profile                       → 내 정보
│   │   ├── /payment-history               → 결제 내역
│   │   │
│   │   └── 📁 고객센터 (Phase 1 신규)
│   │       ├── /support                   → 고객센터 메인
│   │       ├── /support/inquiry           → 1:1 문의 목록
│   │       ├── /support/inquiry/new       → 새 문의 작성
│   │       └── /support/inquiry/[id]      → 문의 상세
│   │
│   └── /pricing                           → 가격 정책 (결제)
│
└── 🔐 관리자 영역 (별도 인증)
    │
    ├── /admin/login                       → 관리자 로그인
    │
    └── 📁 관리자 대시보드
        │
        ├── Phase 1: CS 자동화
        │   ├── /admin                     → 대시보드 홈 (KPI)
        │   ├── /admin/inquiries           → 문의 목록
        │   └── /admin/inquiries/[id]      → 문의 상세 + AI 분석
        │
        └── Phase 2: 운영 도구 + Growth
            ├── /admin/users               → 사용자 관리
            ├── /admin/users/[id]          → 사용자 상세
            ├── /admin/payments            → 결제/환불 관리
            ├── /admin/promos              → 프로모션 코드
            ├── /admin/announcements       → 공지/팝업 관리
            └── /admin/growth              → Growth 설정
```

---

## 2. 사용자 유형별 접근 권한

| 경로 | 비로그인 | 일반 사용자 | 관리자 |
|------|:-------:|:----------:|:------:|
| `/` ~ `/pricing` | ✅ | ✅ | ✅ |
| `/employer/*`, `/worker/*` | ❌ | ✅ | ✅ |
| `/support/*` | ❌ | ✅ | ✅ |
| `/admin/login` | ✅ | ✅ | ✅ |
| `/admin/*` (그 외) | ❌ | ❌ | ✅ |

---

## 3. 페이지별 상세 정보

### 3.1 고객용 - 고객센터 (Phase 1)

| 경로 | 페이지명 | 주요 기능 |
|------|---------|----------|
| `/support` | 고객센터 메인 | FAQ 링크, 1:1 문의 진입점, 미읽음 뱃지 |
| `/support/inquiry` | 문의 목록 | 내 문의 내역, 상태별 필터 |
| `/support/inquiry/new` | 새 문의 작성 | 카테고리 선택, 제목/내용 입력 |
| `/support/inquiry/[id]` | 문의 상세 | 문의 내용, 답변 확인, 읽음 처리 |

### 3.2 관리자용 - Phase 1

| 경로 | 페이지명 | 주요 기능 |
|------|---------|----------|
| `/admin/login` | 로그인 | 비밀번호 입력 → JWT 발급 |
| `/admin` | 대시보드 홈 | 오늘 현황, 통계, 카테고리별 분포 |
| `/admin/inquiries` | 문의 목록 | 상태/카테고리 필터, 검색, 페이지네이션 |
| `/admin/inquiries/[id]` | 문의 상세 | 사용자 정보, AI 분석, 답변 수정/승인 |

### 3.3 관리자용 - Phase 2

| 경로 | 페이지명 | 주요 기능 |
|------|---------|----------|
| `/admin/users` | 사용자 목록 | 검색, 역할 필터, 차단 상태 |
| `/admin/users/[id]` | 사용자 상세 | 정보 조회, 크레딧 지급/차감, 차단/해제 |
| `/admin/payments` | 결제 관리 | 결제 내역, 환불 처리 |
| `/admin/promos` | 프로모션 코드 | 코드 생성/수정/삭제, 사용 현황 |
| `/admin/announcements` | 공지/팝업 | 공지 생성/수정, 노출 기간 설정 |
| `/admin/growth` | Growth 설정 | 알림 ON/OFF, 테스트 발송 |

---

## 4. 네비게이션 구조

### 4.1 고객용 GNB (Global Navigation Bar)

```
┌─────────────────────────────────────────────────────────┐
│  [로고]                      [마이페이지]  [고객센터 🔴] │
└─────────────────────────────────────────────────────────┘
                                               ↑
                                        미읽음 답변 있으면 뱃지
```

### 4.2 관리자용 사이드바

```
┌──────────────────┐
│  DevSupport AI   │
├──────────────────┤
│  📊 대시보드      │  ← /admin
├──────────────────┤
│  💬 문의 관리     │  ← /admin/inquiries
│  👥 사용자        │  ← /admin/users
│  💳 결제/환불     │  ← /admin/payments
│  🎁 프로모션      │  ← /admin/promos
│  📢 공지사항      │  ← /admin/announcements
│  📈 Growth       │  ← /admin/growth
├──────────────────┤
│  [로그아웃]       │
└──────────────────┘
```

---

## 5. 데이터 흐름

### 5.1 고객 문의 플로우

```
[사용자]                    [시스템]                      [관리자]
   │                           │                            │
   │  1. 문의 작성              │                            │
   │ ──────────────────────▶  │                            │
   │                           │  2. DB 저장                │
   │                           │  3. 이메일 알림 ──────────▶│
   │                           │                            │
   │                           │         4. 문의 상세 열기   │
   │                           │◀──────────────────────────│
   │                           │                            │
   │                           │  5. AI 분석 (Lazy)         │
   │                           │  6. 결과 저장               │
   │                           │                            │
   │                           │         7. 답변 승인        │
   │                           │◀──────────────────────────│
   │  8. 답변 알림              │                            │
   │◀──────────────────────── │                            │
   │                           │                            │
   │  9. 읽음 처리              │                            │
   │ ──────────────────────▶  │                            │
```

### 5.2 관리자 인증 플로우

```
[관리자]                    [시스템]
   │                           │
   │  1. /admin 접근 시도       │
   │ ──────────────────────▶  │
   │                           │  2. 세션 확인 (미들웨어)
   │                           │     ↓ 세션 없음
   │  3. /admin/login 리다이렉트│
   │◀──────────────────────── │
   │                           │
   │  4. 비밀번호 입력          │
   │ ──────────────────────▶  │
   │                           │  5. 환경변수와 비교
   │                           │  6. JWT 생성 + 쿠키 저장
   │  7. /admin 리다이렉트      │
   │◀──────────────────────── │
   │                           │
   │  8. /admin 접근            │
   │ ──────────────────────▶  │
   │                           │  9. JWT 검증 (미들웨어)
   │ 10. 대시보드 표시          │     ↓ 성공
   │◀──────────────────────── │
```

---

## 6. 폴더 구조

```
signplease/
├── app/
│   │
│   ├── (public)/                    # 공개 영역
│   │   ├── login/
│   │   ├── signup/
│   │   ├── guest/
│   │   ├── onboarding/
│   │   └── auth/
│   │       ├── callback/
│   │       └── signout/
│   │
│   ├── (protected)/                 # 보호 영역 (로그인 필수)
│   │   ├── employer/
│   │   ├── worker/
│   │   ├── profile/
│   │   ├── payment-history/
│   │   ├── pricing/
│   │   ├── select-role/
│   │   │
│   │   └── support/                 # 🆕 Phase 1
│   │       ├── page.tsx             # 고객센터 메인
│   │       ├── inquiry/
│   │       │   ├── page.tsx         # 문의 목록
│   │       │   ├── new/
│   │       │   │   └── page.tsx     # 새 문의
│   │       │   └── [id]/
│   │       │       └── page.tsx     # 문의 상세
│   │       └── actions.ts
│   │
│   ├── admin/                       # 🆕 관리자 (별도 인증)
│   │   ├── login/
│   │   │   └── page.tsx
│   │   ├── page.tsx                 # 대시보드 홈
│   │   ├── inquiries/               # Phase 1
│   │   │   ├── page.tsx
│   │   │   └── [id]/
│   │   │       └── page.tsx
│   │   ├── users/                   # Phase 2
│   │   │   ├── page.tsx
│   │   │   └── [id]/
│   │   │       └── page.tsx
│   │   ├── payments/                # Phase 2
│   │   │   └── page.tsx
│   │   ├── promos/                  # Phase 2
│   │   │   └── page.tsx
│   │   ├── announcements/           # Phase 2
│   │   │   └── page.tsx
│   │   ├── growth/                  # Phase 2
│   │   │   └── page.tsx
│   │   ├── actions.ts
│   │   └── layout.tsx               # 관리자 레이아웃 (사이드바)
│   │
│   ├── api/
│   │   └── cron/                    # 🆕 Phase 2
│   │       └── retention-alert/
│   │           └── route.ts
│   │
│   └── contract/
│       └── sign/[token]/
│
├── lib/
│   ├── supabase/
│   ├── solapi/
│   ├── cs/                          # 🆕 Phase 1
│   │   ├── ai.ts
│   │   ├── github.ts
│   │   ├── sentry.ts
│   │   ├── context.ts
│   │   └── notifications.ts
│   ├── admin/                       # 🆕 Phase 1
│   │   └── auth.ts
│   ├── notifications/               # 🆕 Phase 2
│   │   └── growth.ts
│   └── payments/                    # 🆕 Phase 2
│       └── refund.ts
│
├── types/
│   ├── database.ts
│   ├── index.ts
│   └── cs.ts                        # 🆕 Phase 1
│
└── docs/
    ├── PRD.md
    ├── IA.md                        # 🆕 이 문서
    └── manuals/                     # 🆕 Phase 1
        ├── faq.md
        ├── payment-errors.md
        ├── refund-policy.md
        └── usage-guide.md
```

---

## 7. API 라우트

### 7.1 기존 API

| Method | 경로 | 용도 |
|--------|------|------|
| POST | `/api/payment/prepare` | 결제 준비 |
| POST | `/api/payment/confirm` | 결제 확인 |
| POST | `/api/ai-review` | AI 계약서 리뷰 |
| POST | `/api/pdf/generate` | PDF 생성 |
| POST | `/api/pdf/career-certificate` | 경력 증명서 |
| GET | `/api/contract/sensitive-info` | 민감정보 조회 |

### 7.2 신규 API (Phase 2)

| Method | 경로 | 용도 |
|--------|------|------|
| GET | `/api/cron/retention-alert` | 이탈 방지 알림 (Vercel Cron) |

---

## 8. 인증 체계

```
┌─────────────────────────────────────────────────────────────┐
│                        인증 레이어                           │
├────────────────────────┬────────────────────────────────────┤
│    일반 사용자 인증      │         관리자 인증                 │
├────────────────────────┼────────────────────────────────────┤
│ Provider: Supabase Auth│ Provider: 커스텀 JWT               │
│ Method: 카카오/애플 OAuth│ Method: 환경변수 비밀번호          │
│ Session: Supabase 쿠키  │ Session: admin_session 쿠키        │
│ 유효기간: 7일 (자동갱신) │ 유효기간: 24시간                   │
├────────────────────────┼────────────────────────────────────┤
│ DB 접근: RLS (본인만)   │ DB 접근: Service Role (전체)       │
└────────────────────────┴────────────────────────────────────┘
```

---

## 9. 개발 우선순위

### Phase 1: CS 자동화 (MVP)

```
1. 기반 작업
   └── /admin/login, /admin (빈 페이지)
   
2. 고객용 UI
   └── /support, /support/inquiry/*
   
3. AI 분석
   └── lib/cs/* 유틸 함수
   
4. 관리자 대시보드
   └── /admin/inquiries/*
```

### Phase 2: 운영 도구 + Growth

```
1. Growth 자동화
   └── /api/cron/retention-alert
   
2. 운영 도구
   └── /admin/users, /admin/payments, /admin/promos
   
3. KPI 대시보드
   └── /admin (대시보드 확장)
   
4. 공지/팝업
   └── /admin/announcements
```

---

*작성일: 2026년 2월 6일*
*PRD 버전 기준: 2026년 2월 6일 (심층 검토 완료)*
